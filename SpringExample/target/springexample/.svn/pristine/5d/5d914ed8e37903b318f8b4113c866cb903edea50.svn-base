#!/bin/bash

CONF_PATH=/search/odin/resin/umis/webapp/WEB-INF/

find $CONF_PATH -name "*.old" | while read i

do
    dir=`dirname $i`
    filename=`basename $i .old`
    echo "name: $i";
    mv $i $dir/$filename
done
